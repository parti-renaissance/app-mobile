private def loadLocalProperties() {
    def properties = new Properties()
    def file = rootProject.file("local.secret.properties")
    if (file.exists()) {
        properties.load(new FileInputStream(file))
    }
    return properties
}

ext.localProperties = loadLocalProperties()
